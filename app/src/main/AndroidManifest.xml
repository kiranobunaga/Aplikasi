<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aplikasi"
        tools:targetApi="31">
        <activity
            android:name=".MatkulActivity"
            android:exported="false" />
        <activity
            android:name=".EditDosenActivity"
            android:exported="false" />
        <activity
            android:name=".LihatDosenActivity"
            android:exported="false" />
        <activity
            android:name=".CreateActivity"
            android:exported="false" />
        <activity
            android:name=".DosenActivity"
            android:exported="false" />
        <activity
            android:name=".MahasiswaActivity"
            android:exported="false"
            android:label="@string/title_activity_mahasiswa"
            android:resizeableActivity="true"
            android:theme="@style/Theme.Aplikasi.NoActionBar"
            tools:targetApi="24" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/gpa"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Aplikasi" />
    </application>

</manifest>